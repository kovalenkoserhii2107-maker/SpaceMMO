# Space Strategy MMO

Браузерная мультиплеерная экономическая стратегия в сеттинге космоса: колонии на планетах,
добыча и энергобаланс, дерево технологий, верфь, логистика флота между системами,
биржа за криптогривну, синдикаты с общим банком, PvE-экспедиции и детерминированные бои.

Все расчеты — на сервере. Клиент только рисует состояние и отправляет намерения.

**Стек:** Node.js + TypeScript (strict, ESM) · Express 5 · Socket.IO ·
PostgreSQL (Prisma 7 + `@prisma/adapter-pg`) · HTML/JS/CSS без сборщика · Docker.

## Локальный запуск

Нужны Node.js 24 (как в `Dockerfile`) и доступная база PostgreSQL (подойдет Neon или локальный Postgres).

```bash
npm install
```

Скопируй пример окружения и заполни его:

```bash
cp .env.example .env
```

| Переменная | Обязательна | Назначение |
| --- | --- | --- |
| `DATABASE_URL` | да | строка подключения к PostgreSQL |
| `JWT_SECRET` | в production | секрет подписи токенов сессии |
| `PORT` | нет | порт сервера, по умолчанию 3000 |
| `GOOGLE_CLIENT_ID`, `APPLE_CLIENT_ID`, `FACEBOOK_APP_ID` | нет | ключи OAuth, пока не подключены |

Примени миграции, сгенерируй галактику и подними dev-сервер:

```bash
npx prisma migrate dev
```

```bash
npm run generate
```

```bash
npm run dev
```

Клиент откроется на http://localhost:3000 — там же регистрация, создание командира
и выдача стартовой колонии.

### Docker

```bash
docker build -t space-mmo . && docker run --env-file .env -p 3000:3000 space-mmo
```

## Скрипты

| Команда | Назначение |
| --- | --- |
| `npm run dev` | dev-сервер с автоперезапуском (tsx watch) |
| `npm run build` / `npm start` | сборка и запуск production-версии |
| `npm run typecheck` | строгая проверка типов |
| `npm run audit` | поиск мертвого кода: неиспользуемые переменные и параметры |
| `npm run generate` | генерация галактики: 10-15 систем, планеты и торговые хабы |
| `npm run check:rules` | sanity-check игровых формул (добыча, энергия, бой, экспедиции) |
| `npm run prisma:migrate` | миграции БД |
| `npm run prisma:generate` | перегенерация Prisma-клиента |
| `npm run legacy:password` | задать пароль аккаунту, мигрировавшему до появления авторизации |
| `npm run test:stand` | подготовить стенд для тестов и напечатать их конфигурацию |

## Фичи

**Аккаунт и командир.** Регистрация и вход по email с паролем (scrypt с солью,
сравнение постоянным по времени), сессия — JWT: один и тот же токен для REST
и для рукопожатия WebSocket. Смена пароля одноразовым кодом с истечением; ответ
одинаков для существующего и несуществующего email. Кнопки Google/Apple/Facebook
есть, но без ключей сервер честно отвечает 501. Учетная запись отделена от игровой
личности: `User` отвечает за вход, `Commander` — за позывной, аватар, криптогривну
и счетчики боев. Достижения («Первый шаг», «Архитектор», «Гроза пиратов») выдаются
идемпотентно после сохранения состояния.

**Колонии и экономика.** Планеты с индивидуальными коэффициентами металла, кристаллов,
дейтерия и инсоляции. Здания тратят энергию: при дефиците добыча всех шахт падает
пропорционально (`efficiency = output / usage`). Дерево из технологий с иерархией
требований — энергетика, вычислительная техника, горное дело, реактивный двигатель,
гиперпространственная физика, гипердвигатель, астрофизика. Верфь строит корабли
и стационарную оборону собственными очередями.

**Тиковый движок.** Стройка, исследования и очереди считаются от абсолютных меток
времени: процессы идут, пока игрок офлайн, и переживают перезапуск сервера. Добыча
за офлайн (до 24 ч) начисляется по отрезкам между завершениями таймеров. Тик раз
в секунду, пакетная запись в БД, push состояния клиенту по WebSocket.

**Карта и логистика.** Микро-карта системы (SVG со звездой и орбитами) и макро-карта
галактики с координатами, классом звезды и аномалиями. Туман войны: своя планета видна
целиком и в реальном времени, чужая — только название, орбита, тип и размер; после
прилета зонда сохраняется стареющий снимок разведки. Скорость флота задает самый
медленный корабль, грузоподъемность и топливо считаются по составу и времени в пути.
Внутри системы флот идет на дейтерии, между системами — гиперпрыжком на антиматерии.
Прилеты обрабатываются глобальным сканом: груз доходит и корабли возвращаются,
даже если обе стороны офлайн.

**Антиматерия и аномалии.** «Синтезатор антиматерии» — самая дорогая постройка
с огромным расходом энергии, синтез идет на порядки медленнее обычной добычи.
Аномалия «Черная дыра» в 1-2 системах дает «Искажение времени»: синтез антиматерии
+50%, стройка (включая верфь) и исследования на 30% дольше.

**Биржа и криптогривна.** Торговый хаб — нейтральная станция на орбите у звезды.
Товар попадает на склад только транспортным флотом, что не влезло — возвращается
домой в трюмах. Биржевой стакан: ордера на покупку и продажу металла и кристаллов,
частичное исполнение, залог товара и денег. Сделки идут одной транзакцией БД
и пишутся в журнал.

**Синдикаты.** Название, тег, лидер и состав; основание стоит криптогривну. RBAC:
лидер и офицеры разбирают заявки, состав и роспуск — только лидер, офицер исключает
лишь рядовых. Передача лидерства делает старого лидера офицером одной транзакцией.
Общий банк: пожертвование списывается со счета и зачисляется в банк атомарно,
каждая операция попадает в журнал, при роспуске остаток возвращается лидеру.
Дипломатия альянсов: члены синдиката не объявляют личных войн — войну и мир
объявляют лидер или офицеры сразу вражескому синдикату.

**Экспедиции (PvE).** Вылет в «глубокий космос» — абстрактную 16-ю позицию, которая
есть у каждой системы. «Астрофизика» задает число одновременных экспедиций, повышает
находки и дает шанс уйти от засады. Событийный движок бросает кубик по весам: мертвая
тишина, заброшенный груз, уклонение или бой с пиратами. Генератор случайных чисел
передается снаружи, поэтому любой исход воспроизводим в тестах.

**Бой.** Три типа урона (кинетический, лазерный, ионный) против трех слоев защиты
(щиты, броня, корпус). Урон проходит слои по порядку; ионный вдвое эффективнее против
щитов и вдвое слабее против брони, кинетический — ровно наоборот, лазерный ровен везде.
Получается «камень-ножницы-бумага»: дорогой кинетический флот вязнет в дешевых ионных
фрегатах. Проигравший теряет всё, победитель — долю, равную отношению сил. Победивший
атакующий вывозит до половины склада, но не больше вместимости уцелевших трюмов.
Отчеты показывают, куда ушел урон с обеих сторон. Расчет детерминированный:
50 прогонов одного состава дают один результат.

## Структура

```
src/
  config/env.ts                 загрузка и валидация окружения
  config/auth.ts                секрет JWT, сроки жизни токенов, провайдеры
  db/prisma.ts                  PrismaClient с pg-адаптером
  game/rules.ts                 чистые формулы: добыча, стоимость, энергия, эффективность
  game/techTree.ts              дерево технологий: стоимость, время, требования, бонусы
  game/ships.ts                 классы кораблей: стоимость, время, боевые профили
  game/defenses.ts              стационарная оборона: стоимость, время, энергия
  game/combat.ts                бой: типы урона, слои защиты, потери, грабеж
  game/fleets.ts                логистика: орбиты, гиперпрыжки, трюмы, топливо
  game/fogOfWar.ts              видимость планет и снимки разведки
  game/market.ts                вместимость складов и проверки ордеров
  game/expeditions.ts           событийный движок экспедиций и лимит слотов
  game/baseState.ts             состояние игрока и баз в памяти + снимки для клиента
  game/gameLoop.ts              изолированный тик: таймеры, очереди, догон офлайна
  routes/                       REST + middleware авторизации и разбор ввода
  services/authService.ts       пароли, JWT, смена пароля, заготовка OAuth
  services/commanderService.ts  профиль командира и онбординг
  services/achievementService.ts каталог достижений и их выдача
  services/mapService.ts        сборка карты системы под конкретного игрока
  services/marketService.ts     биржа: ордера, сделки, склады хаба (в транзакциях)
  services/syndicateService.ts  синдикаты: роли, заявки, общий банк
  services/warService.ts        дипломатия и отчеты о боях
  types/api.ts                  типы ответов REST
  types/socket.ts               типизированные интерфейсы Socket.IO
  scripts/                      генерация галактики, проверка формул, стенд для тестов
public/                         клиент: вход, командный центр, карты, биржа, синдикаты
prisma/                         схема и миграции
tests/                          стресс-тесты по живому API
```

## API

| Метод | Путь | Назначение |
| --- | --- | --- |
| POST | `/api/auth/register` | регистрация по email и паролю |
| POST | `/api/auth/login` | вход, выдача JWT |
| POST | `/api/auth/oauth/:provider` | вход через Google/Apple/Facebook (ждет ключей) |
| POST | `/api/auth/password/reset-request` | запрос кода смены пароля |
| POST | `/api/auth/password/reset` | установка нового пароля |
| GET | `/api/auth/me` | текущая сессия: аккаунт, командир, аватары |
| POST | `/api/auth/commander` | создание командира (онбординг) |
| GET | `/api/state` | состояние игрока: базы, очереди, технологии, флот |
| POST | `/api/bases/:id/build` | поставить здание в стройку |
| POST | `/api/bases/:id/research` | запустить исследование |
| POST | `/api/bases/:id/ships` | заказать корабли на верфи |
| POST | `/api/bases/:id/defenses` | заказать стационарную оборону |
| GET | `/api/galaxy` | макро-карта: все системы с координатами |
| GET | `/api/map?systemId=` | карта системы (по умолчанию — родной) с туманом войны |
| POST | `/api/bases/:id/fleets/preview` | предрасчет маршрута: время, топливо, трюмы |
| POST | `/api/bases/:id/fleets` | отправить флот (транспорт, разведка, хаб, атака, экспедиция) |
| GET | `/api/market` | склад на хабе, стакан, свои ордера, журнал сделок |
| POST | `/api/market/storage/upgrade` | расширить склад на хабе |
| POST | `/api/market/orders` | выставить ордер на покупку или продажу |
| POST | `/api/market/orders/:id/fill` | исполнить чужой ордер |
| DELETE | `/api/market/orders/:id` | снять свой ордер |
| GET | `/api/war` | дипломатия, отчеты о боях и экспедициях |
| POST | `/api/war/declare` | объявить войну (одиночки) |
| POST | `/api/war/peace` | заключить мир |
| POST | `/api/war/syndicate/declare` | объявить войну синдикату (лидер/офицер) |
| POST | `/api/war/syndicate/peace` | заключить мир между синдикатами |
| GET | `/api/syndicates` | список синдикатов, свой синдикат, банк и журнал |
| POST | `/api/syndicates` | основать синдикат |
| POST | `/api/syndicates/:id/apply` | подать заявку на вступление |
| POST | `/api/syndicates/applications/:id/approve` | одобрить заявку (лидер/офицер) |
| POST | `/api/syndicates/applications/:id/reject` | отклонить заявку (лидер/офицер) |
| POST | `/api/syndicates/members/:id/kick` | исключить участника |
| POST | `/api/syndicates/members/:id/role` | назначить офицера или передать лидерство |
| POST | `/api/syndicates/donate` | пожертвование в общий банк |
| POST | `/api/syndicates/leave` | покинуть синдикат |
| POST | `/api/syndicates/disband` | распустить синдикат (лидер) |

Экспедиция отправляется тем же `POST /api/bases/:id/fleets` с миссией `EXPEDITION`:
без цели — в глубокий космос родной системы, с `targetSystemId` — в чужую.

## Тесты

Скрипты в [tests/](tests/README.md) бьют по живому API работающего dev-сервера
и проверяют, что эксплойты закрыты: валидация ввода, гонки на бирже, права ролей
в синдикатах, атомарность банка, боевые профили юнитов. Всего 122 проверки.

```bash
npm run --silent test:stand > stand.json
```

```bash
node tests/auth.mjs
```

```bash
node tests/stress-market-fleet.mjs stand.json
```

```bash
node tests/stress-races.mjs stand.json
```

```bash
node tests/syndicates.mjs stand.json
```

## Качество кода

- Строгий TypeScript: `strict`, `noUncheckedIndexedAccess`, `exactOptionalPropertyTypes`.
  Отдельный прогон `npm run audit` добавляет `noUnusedLocals` и `noUnusedParameters`.
- Ответы REST типизированы через `Response<...>` ([types/api.ts](src/types/api.ts)),
  события Socket.IO — через типовые контракты ([types/socket.ts](src/types/socket.ts)):
  расхождение между сервером и клиентом ловит компилятор.
- Игровые правила вынесены в чистые модули без обращений к БД, поэтому формулы
  проверяются отдельно от инфраструктуры (`npm run check:rules`).
- Экономика, бой и логистика идут транзакциями: гонки закрыты условными `UPDATE`,
  жесткий перезапуск между шагами не задваивает начисления и не теряет корабли.

Правила разработки и текущая архитектура — в [CLAUDE.md](CLAUDE.md).
