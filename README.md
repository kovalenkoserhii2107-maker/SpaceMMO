# Space Strategy MMO — Этап 1

Браузерная мультиплеерная экономическая стратегия в космосе.
Реализован **Этап 1**: фундамент, генерация системы и базовая добыча.

## Стек
Node.js + TypeScript · Express · Socket.IO · PostgreSQL (Prisma 7 + pg adapter) · HTML/JS/CSS · Docker

## Запуск

```bash
npm install
cp .env.example .env        # укажи DATABASE_URL
npx prisma migrate dev      # применить миграции
npm run generate            # сгенерировать стартовую систему (3-5 планет)
npm run dev                 # http://localhost:3000
```

## Скрипты
| Команда | Назначение |
| --- | --- |
| `npm run dev` | dev-сервер с автоперезапуском |
| `npm run build` / `npm start` | сборка и запуск production-версии |
| `npm run typecheck` | строгая проверка типов |
| `npm run generate` | генерация стартовой солнечной системы |
| `npm run check:rules` | sanity-check формул добычи и энергии |
| `npm run prisma:migrate` | миграции БД |

## Что реализовано (Этап 1)
- **Схемы БД:** `User`, `SolarSystem`, `Planet` (коэффициенты Металл/Кристаллы/Дейтерий/Инсоляция), `Base`.
- **Генерация:** одна стартовая система, 3-5 уникальных планет с типами и разбросом богатства.
- **Авторизация (заглушка):** вход по позывному, новому игроку выдается свободная планета и колония.
- **Командный центр:** дашборд со списком баз, параметрами планеты, ресурсами и инфраструктурой.
- **Game Loop:** изолированный серверный тик раз в 1 секунду, начисление ресурсов, догон офлайн-периода
  (до 24 ч), пакетная запись в БД раз в 10 тиков, push состояния в клиент через WebSocket.
- **Постройка:** проверка ресурсов и энергобаланса строго на сервере, мгновенный апгрейд уровня.

## Архитектура
```
src/
  config/env.ts        загрузка и валидация окружения
  db/prisma.ts         PrismaClient с pg-адаптером
  game/rules.ts        чистые формулы: добыча, стоимость, энергия
  game/baseState.ts    состояние базы в памяти + снимок для клиента
  game/gameLoop.ts     изолированный тик, не блокирует API
  routes/              REST: авторизация, состояние, постройка
  services/            учетные записи и выдача стартовой планеты
  types/socket.ts      типизированные интерфейсы Socket.IO
  scripts/             генерация системы, проверка формул
public/                клиент: логин + командный центр
```

## Дальше (заблокировано)
Этапы 2-5 (технологии, верфь, логистика, биржа, бой) не реализуются до утверждения Этапа 1.
